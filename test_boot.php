<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Http\Kernel::class); $start = microtime(true); $response = $kernel->handle(Illuminate\Http\Request::capture()); $end = microtime(true); echo 'Time: ' . ($end - $start) . 's'; 
